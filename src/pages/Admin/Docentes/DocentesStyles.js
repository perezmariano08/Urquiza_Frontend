import styled from "styled-components";

export const DocentesMain = styled.div`
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 20px;
    gap: 20px;
`

export const DocenteHeader = styled.div`
    display: flex;
    flex-direction: column;
    gap: 10px;
    background-color: var(--white-0);
    border: 1px solid var(--white-100);
    padding: 20px;
`